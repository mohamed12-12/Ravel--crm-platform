# Packages

Reserved for shared packages extracted from the apps and services.

- `shared/` is intended for cross-runtime types, contracts, constants, and validation helpers.
- `ui/` is intended for reusable admin UI components.

No shared package runtime behavior has been introduced in this cleanup phase.
