# Apps

Deployable application surfaces live here.

- `admin-web/` is the React/Vite operator UI prototype.
- `api/` is the Flask CRM with database-backed admin screens and API routes.
- `middleware/` is the TypeScript API layer for orchestration and future channel integrations.

Keep public routes and UI behavior stable during cleanup-only refactors.
