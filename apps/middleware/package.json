{
  "name": "@rahma-travel-os/middleware",
  "version": "1.0.0",
  "description": "Deterministic Middleware for Rahma Traveler CRM",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('No middleware automated tests yet. Add route and service tests before production.')\"",
    "prisma:generate": "prisma generate --schema ../../database/schema/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema ../../database/schema/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.6.2",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "prisma": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
