# Middleware Source

Source for the TypeScript middleware.

- `index.ts` wires Express, middleware, and routes.
- `controllers/` contains HTTP controllers.
- `services/` contains service-layer helpers and future integration logic.
