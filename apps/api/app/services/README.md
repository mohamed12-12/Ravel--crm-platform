# CRM Services

Service helpers for import, identity resolution, and deterministic copy rendering.

Shared workflow behavior that must also be used by the demo app generally belongs in `services/crm/system_services/`.
