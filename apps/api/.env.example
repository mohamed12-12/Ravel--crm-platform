# Optional apps/api local overrides.
# Prefer the root ../.env for shared local development settings.

FLASK_CONFIG=development
FLASK_ENV=development
SECRET_KEY=
DATABASE_URL=sqlite:///rahma_traveler_dev.db
# Explicit staging PostgreSQL target for migration/validation scripts only.
POSTGRES_URL=
USD_TO_EGP_RATE=50.0
ADMIN_USERNAME=
ADMIN_PASSWORD=
EXCEL_FILE_PATH=data/travelers_database.xlsx
GOOGLE_SHEET_ID=
GOOGLE_CREDS_PATH=
DATA_AUTHORITY=crm
DATA_SCHEMA_VERSION=1
CRM_ACCESS_MODE=shared_service
CRM_API_BASE_URL=
CRM_API_TOKEN=
# Role granted to CRM_API_TOKEN callers, resolved server-side (the client's
# X-CRM-Role header is no longer trusted). Defaults to "agent" if unset.
CRM_API_TOKEN_ROLE=agent
EXCEL_IMPORT_ENABLED=true
EXCEL_EXPORT_ENABLED=true
GOOGLE_SHEETS_IMPORT_ENABLED=false
GOOGLE_SHEETS_EXPORT_ENABLED=false
DIRECT_IMPORT_APPLY_ENABLED=false
CRM_SHEET_MIRROR_ENABLED=false
DEMO_DATA_MODE=true
