# Admin Source

Source for the React admin UI.

Current modules:

- `App.tsx` contains the prototype shell and screens.
- `api.ts` contains the Axios client for middleware calls.
- `main.tsx` mounts the app.
