# AI Agent Service

The spreadsheet-backed MVP agent lives in `ai_agent_app/`.

It owns the current demo conversation flow, sheet gateway adapters, demo UI templates, and compatibility routes used by the regression tests. Preserve route and workflow behavior during cleanup-only changes.
