# Agent Logic

This folder owns the MVP agent session state machine.

The agent coordinates intake, traveler matching, trip selection, booking draft collection, and human handoff messaging. Business behavior is covered by the Python regression tests, so refactors here should be small and test-driven.
