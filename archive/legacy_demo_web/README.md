# Legacy Demo Wrapper

`demo_web` is retained as a compatibility wrapper around `services.ai_agent.ai_agent_app.server:create_app`.

Several tests and older commands still import `demo_web.app`. Do not delete or rename this folder until those contracts are migrated.
