# Security Remediation Plan

Remove debug password fallback; protect all write/upload/view/reset/export routes with roles and CSRF; redact webhook tokens; add rate limits and replay protection; secure passport storage; rotate exposed credentials; add secret/SAST/dependency scanning gates.
