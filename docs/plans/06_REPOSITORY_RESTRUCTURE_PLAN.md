# Repository Restructure Plan

Classify `demo_web` as legacy; consolidate application ownership only after compatibility review; separate operational scripts from phase history; group tests by type after CI support exists; move generated data/artifacts into explicit owned locations. Assess imports, config, tests and deployment before every move.
